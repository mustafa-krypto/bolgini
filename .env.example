# Bolgini Contact Forms Server Configuration
# Copy this file to .env and fill in your actual values

# Email Configuration (Required)
EMAIL_USER=your-gmail-account@gmail.com
EMAIL_PASS=your-app-specific-password

# Target Email (Where forms will be sent)
TO_EMAIL=info@bolgini.com

# Azure OpenAI Configuration (Required for AI Spam Detection)
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_DEPLOYMENT_NAME=your-gpt-4-deployment-name
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Server Configuration
PORT=3191

# Optional: Additional Email Recipients (comma-separated)
# CC_EMAILS=sales@bolgini.com,support@bolgini.com

# Security Settings (Optional - defaults are set)
# RATE_LIMIT_WINDOW=900000  # 15 minutes in ms
# MAX_REQUESTS_PER_WINDOW=5

# Environment
NODE_ENV=production

# Setup Instructions:
# For Gmail SMTP:
# 1. Enable 2-factor authentication on your Google account
# 2. Generate an "App Password" for this application
# 3. Use the app password (not your regular password) in EMAIL_PASS

# For Azure OpenAI:
# 1. Create Azure OpenAI resource in Azure portal
# 2. Deploy GPT-4 or GPT-4 Turbo model
# 3. Get the endpoint, API key, and deployment name from Azure portal
